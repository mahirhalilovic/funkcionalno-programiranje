// Napisati let izraz koji ce rezultirati stringom "SAMOGLASNIK" ukoliko expr1 (char)
// sadrzi samoglasnik. U suprotnom, izraz ce da generise runtime iznimku.

let c = 'o'
let 'a' | 'e' | 'i' | 'o' | 'u' = c in "SAMOGLASNIK"